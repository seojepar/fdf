#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv)
{
    /*
    컴파일이 되지 않는 코드 => 왜? 모르겠다.
    int *test = malloc(20);
    test[2][4] = 3;
    printf("%d", test[4][2]);

    int **test = malloc(20);
    test[4][5] = 3;
    printf("%d", test[3][4]);
    작동은 되지만 세그폴트가 나는 코드. 지정하지 않은 곳에 접근하였으므로. 
    array로 접근할 경우 에러가 나지는 않는다.
    */
}